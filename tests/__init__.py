"""LineageMedic test suite.

This file exists so the tests form an importable package (``tests.*``), which
lets mypy apply the per-module override in the root pyproject. pytest discovers
them either way via the configured ``pythonpath``.
"""
